name: Deploy to Cloud Run
on:
  push:
    branches:
      - main
      - feature/gcp-deploy
jobs:
  cloudrunProd:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build Docker Image
        run: |-
          docker build -t southamerica-east1-docker.pkg.dev/moura-366701/meliapi -t latest .

      - name: Login to GCR
        uses: docker/login-action@v1
        with:
          registry: gcr.io
          username: _json_key
          password: ${{ secrets.SERVICE_ACCOUNT_KEYS }}

      - name: Push to Container Registry
        run: |-
          docker push southamerica-east1-docker.pkg.dev/moura-366701/meli/api

      - name: Deploy to Cloud Run
        id: deploy
        uses: google-github-actions/deploy-cloudrun@main
        with:
          service: meli-api
          image: southamerica-east1-docker.pkg.dev/moura-366701/meli/api
          project_id: moura-366701
          credentials: ${{ secrets.SERVICE_ACCOUNT_KEYS }}